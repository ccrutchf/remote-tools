name: .NET

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-restore --verbosity normal
      
  publish-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Publish
      run: dotnet publish -p:PublishTrimmed=true --self-contained --runtime win-x86 -c Release -o dist/win-x86
    - name: Archive production artifacts
      uses: actions/upload-artifact@v2
      with:
        name: win-x86
        path: dist/win-x86
      
  publish-linux:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Publish
      run: dotnet publish -p:PublishTrimmed=true --self-contained --runtime linux-x64 -c Release -o dist/linux-x64
    - name: Archive production artifacts
      uses: actions/upload-artifact@v2
      with:
        name: linux-64
        path: dist/linux-64
        
  publish-macos:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 5.0.x
    - name: Publish x64
      run: dotnet publish -p:PublishTrimmed=true --self-contained --runtime osx-64 -c Release -o dist/macos-x64
    - name: Publish arm64
      run: dotnet publish -p:PublishTrimmed=true --self-contained --runtime osx.11.0-arm64 -c Release -o dist/macos-arm64
    - name: Archive x64 production artifacts
      uses: actions/upload-artifact@v2
      with:
        name: macos-x64
        path: dist/macos-x64
    - name: Archive arm64 production artifacts
      uses: actions/upload-artifact@v2
      with:
        name: macos-arm64
        path: dist/macos-arm64
